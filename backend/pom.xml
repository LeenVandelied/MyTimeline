<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.16</version>
		<relativePath/>
	</parent>
	<groupId>com.matimeline</groupId>
	<artifactId>eventmanager</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>eventmanager</name>
	<description>Event manager project</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<!--
			Épingle la version d'API Docker utilisée par docker-java (le client de
			Testcontainers). Sa valeur par défaut tombe HORS de la plage supportée
			par les moteurs Docker récents (Docker Desktop 4.x / Engine 29.x exposent
			MinAPIVersion 1.44) : la requête /v1.xx/info renvoie alors HTTP 400 et
			Testcontainers conclut « Could not find a valid Docker environment ».
			1.44 = plancher supporté par tout moteur ≥ 25.0. Injectée dans le JVM
			forké par surefire (cf. build/plugins). Surcharge : -Ddocker.api.version=1.51.
			NB : plus AUCun override de testcontainers.version — le BOM Boot 3.5.16
			manage désormais Testcontainers 1.21.4 (docker-java récent, compatible),
			l'override 1.20.6 (motivé sous les vieux BOM 3.2.x/3.4.x) est devenu superflu.
		-->
		<docker.api.version>1.44</docker.api.version>
		<!--
			jjwt (#162) — NON géré par le BOM Spring Boot : version fixée ici.
			Passage 0.11.5 → 0.13.0 (correctif CVE + fin de support 0.11.x).
			⚠ API BREAKING depuis 0.12.0 : Jwts.parserBuilder()→Jwts.parser(),
			setSigningKey(Key)→verifyWith(SecretKey), parseClaimsJws()→parseSignedClaims(),
			getBody()→getPayload(), builder setSubject/setId/... dépréciés au profit de
			subject()/id()/issuedAt()/expiration(), signWith(key, SignatureAlgorithm)→
			signWith(SecretKey) (algo HS256 déduit de la taille de clé). Cf. JwtService.
		-->
		<jjwt.version>0.13.0</jjwt.version>
		<!--
			UPGRADE PLATEFORME Boot 3.4.13 → 3.5.16 (#260) — résout les 3 CVE HIGH
			Boot acceptées en S31 (CVE-2026-40973 fix 3.5.14+, CVE-2026-22731 /
			CVE-2026-22733 fix 3.5.12+). Le bump du parent BOM rend TOUS les overrides
			de version #180/#223 superflus : le BOM 3.5.16 manage déjà des versions
			≥ aux correctifs visés, donc les overrides ont été RETIRÉS (aucun skew).
			Versions EFFECTIVES managées par le BOM 3.5.16 (cf. #224 garde anti-drift) :
			  • spring-security 6.5.11   (couvre CVE-2026-22732 / CVE-2025-41232, ≥6.5.9)
			  • spring-framework 6.2.19  (aligné SS 6.5.11)
			  • tomcat-embed 10.1.55     (couvre CVE-2026-41293/43512/43515, ≥10.1.55)
			  • jackson-bom 2.21.4       (couvre CVE-2026-54512/54513, ≫2.18.8)
			  • postgresql 42.7.11       (couvre CVE-2026-42198)
			  • flyway 11.7.2            (module flyway-database-postgresql requis, présent)
			  • testcontainers 1.21.4    (docker-java récent, ≥ ex-override 1.20.6)
			Seuls docker.api.version et jjwt.version (hors BOM) restent fixés ci-dessus.
		-->
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!--
			Actuator (#37) — expose /actuator/health pour le healthcheck Docker
			(docker-compose depends_on: condition: service_healthy). Par défaut,
			seul l'endpoint `health` est exposé sur le web (management.endpoints.web.
			exposure.include=health) et show-details=never : le corps se limite à
			{"status":"UP"} sans fuite d'info interne. L'endpoint est whitelisté
			PUBLIC dans SecurityConfig (permitAll sur /actuator/health) sinon le
			probe reçoit 401.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!--
			Flyway (#42, #162) — migrations versionnées. Spring Boot 3.5.16 gère la
			version via son BOM : Flyway 11.7.2 (#260 : était 10.20.1 sous Boot 3.4.x).
			En Flyway 10+/11+, le support Postgres a QUITTÉ flyway-core → il faut le
			module dédié flyway-database-postgresql, sinon le démarrage Flyway casse
			("Unsupported Database: PostgreSQL" / "no database module"). DEC-S3-001.
			Version alignée sur le BOM Boot (pas de surcharge).
		-->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!--
			Testcontainers (#…) — les tests @SpringBootTest démarrent désormais
			contre un conteneur Postgres JETABLE (même dialecte que la prod, Flyway
			rejoue V1/V2 from scratch), plus contre la base dev partagée localhost:5432.
			Version 100% managée par le BOM Spring Boot 3.5.16 (1.21.4) — plus
			aucun override testcontainers.version (retiré en #260, cf. note l.39).
		-->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>
		<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt-api</artifactId>
				<version>${jjwt.version}</version>
		</dependency>
		<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt-impl</artifactId>
				<version>${jjwt.version}</version>
				<scope>runtime</scope>
		</dependency>
		<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt-jackson</artifactId>
				<version>${jjwt.version}</version>
				<scope>runtime</scope>
		</dependency>
		<!--
			Bucket4j core — in-memory token-bucket rate limiting (no Redis/JCache).
			Buckets live in a ConcurrentHashMap inside RateLimitingFilter, so the
			limits are PER JVM INSTANCE: behind a load balancer with N replicas the
			effective ceiling is N x the configured rate. Acceptable for the current
			single-instance deployment; move to a distributed backend (Redis +
			bucket4j-redis) when scaling out.
		-->
		<dependency>
				<groupId>com.bucket4j</groupId>
				<artifactId>bucket4j-core</artifactId>
				<version>8.10.1</version>
		</dependency>
		<!--
			ArchUnit (#166) — verrouille les règles hexagonales par analyse bytecode
			(test unitaire PUR : aucun conteneur/Spring context, reste rapide en CI).
			Version fixée ici (NON gérée par le BOM Spring Boot). Les violations
			historiques sont gelées via FreezingArchRule (baseline sous
			src/test/resources/archunit_store/) : seule une NOUVELLE violation casse le build.
		-->
		<dependency>
				<groupId>com.tngtech.archunit</groupId>
				<artifactId>archunit-junit5</artifactId>
				<version>1.3.0</version>
				<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.11.0</version>
					<configuration>
							<source>21</source>
							<target>21</target>
							<annotationProcessorPaths>
									<path>
											<groupId>org.projectlombok</groupId>
											<artifactId>lombok</artifactId>
											<version>1.18.30</version>
									</path>
							</annotationProcessorPaths>
					</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<systemPropertyVariables>
						<!--
							Passé au JVM de test pour que docker-java (Testcontainers)
							négocie une version d'API Docker supportée par le moteur
							local. Sans ça, détection Docker en échec (HTTP 400 sur /info).
						-->
						<api.version>${docker.api.version}</api.version>
					</systemPropertyVariables>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<repositories>
		<repository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</pluginRepository>
	</pluginRepositories>

</project>

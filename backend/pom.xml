<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.2.2</version>
		<relativePath/>
	</parent>
	<groupId>com.matimeline</groupId>
	<artifactId>eventmanager</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>eventmanager</name>
	<description>Event manager project</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<!--
			Testcontainers : on surcharge la version managée par le BOM Spring Boot
			3.2.2 (1.19.3) vers 1.20.6. La 1.19.3 embarque docker-java 3.3.4 qui
			négocie une version d'API trop ancienne et se fait rejeter en HTTP 400
			par les moteurs Docker récents (Docker Desktop ≥ 4.x, Engine 29.x,
			MinAPIVersion 1.44). La 1.20.6 embarque un docker-java récent compatible.
		-->
		<testcontainers.version>1.20.6</testcontainers.version>
		<!--
			Épingle la version d'API Docker utilisée par docker-java (le client de
			Testcontainers). Sa valeur par défaut tombe HORS de la plage supportée
			par les moteurs Docker récents (Docker Desktop 4.x / Engine 29.x exposent
			MinAPIVersion 1.44) : la requête /v1.xx/info renvoie alors HTTP 400 et
			Testcontainers conclut « Could not find a valid Docker environment ».
			1.44 = plancher supporté par tout moteur ≥ 25.0. Injectée dans le JVM
			forké par surefire (cf. build/plugins). Surcharge : -Ddocker.api.version=1.51.
		-->
		<docker.api.version>1.44</docker.api.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<!--
			Flyway (#42) — migrations versionnées. Spring Boot 3.2.2 gère la version
			via son BOM : Flyway 9.22.3. En Flyway 9.x, le support Postgres est
			INCLUS dans flyway-core ; le module dédié flyway-database-postgresql
			n'existe pas en 9.x (il n'apparaît qu'à partir de Flyway 10). On garde
			donc uniquement flyway-core, sans surcharger la version (alignée BOM).
			NB : si migration future vers Spring Boot 3.3+/Flyway 10+, AJOUTER
			flyway-database-postgresql (le support Postgres quitte le core en 10).
		-->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!--
			Testcontainers (#…) — les tests @SpringBootTest démarrent désormais
			contre un conteneur Postgres JETABLE (même dialecte que la prod, Flyway
			rejoue V1/V2 from scratch), plus contre la base dev partagée localhost:5432.
			Version pilotée par la property testcontainers.version (cf. <properties>,
			surchargée à 1.20.6 pour compatibilité moteur Docker récent).
		-->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>
		<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt-api</artifactId>
				<version>0.11.5</version>
		</dependency>
		<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt-impl</artifactId>
				<version>0.11.5</version>
				<scope>runtime</scope>
		</dependency>
		<dependency>
				<groupId>io.jsonwebtoken</groupId>
				<artifactId>jjwt-jackson</artifactId>
				<version>0.11.5</version>
				<scope>runtime</scope>
		</dependency>
		<!--
			Bucket4j core — in-memory token-bucket rate limiting (no Redis/JCache).
			Buckets live in a ConcurrentHashMap inside RateLimitingFilter, so the
			limits are PER JVM INSTANCE: behind a load balancer with N replicas the
			effective ceiling is N x the configured rate. Acceptable for the current
			single-instance deployment; move to a distributed backend (Redis +
			bucket4j-redis) when scaling out.
		-->
		<dependency>
				<groupId>com.bucket4j</groupId>
				<artifactId>bucket4j-core</artifactId>
				<version>8.10.1</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.11.0</version>
					<configuration>
							<source>21</source>
							<target>21</target>
							<annotationProcessorPaths>
									<path>
											<groupId>org.projectlombok</groupId>
											<artifactId>lombok</artifactId>
											<version>1.18.30</version>
									</path>
							</annotationProcessorPaths>
					</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<systemPropertyVariables>
						<!--
							Passé au JVM de test pour que docker-java (Testcontainers)
							négocie une version d'API Docker supportée par le moteur
							local. Sans ça, détection Docker en échec (HTTP 400 sur /info).
						-->
						<api.version>${docker.api.version}</api.version>
					</systemPropertyVariables>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<repositories>
		<repository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>spring-milestones</id>
			<name>Spring Milestones</name>
			<url>https://repo.spring.io/milestone</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</pluginRepository>
	</pluginRepositories>

</project>

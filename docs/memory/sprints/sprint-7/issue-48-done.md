RETOUR :
- commits: [464128f]
- resume: Issue #48 — TanStack Query v5.101.2 (@tanstack/react-query + react-query-devtools) installé via npm (lockfile committé). QueryProvider client (`src/contexts/QueryProvider.tsx`) : QueryClient lazy via useState, defaults v5 STRICT (gcTime 5min — PAS cacheTime, staleTime 30s, refetchOnWindowFocus off, retry 1), ReactQueryDevtools dev-only. Inséré dans app/layout.tsx au marqueur EXACT entre AuthProvider et {children} → ordre final Theme > Auth > Query > children (Theme/Auth/Toaster non déplacés). query-keys.ts : conventions hiérarchiques par domaine (products.all/detail/withEvents, events.all/detail, categories.all/detail, auth.me) toutes `as const`. 2 hooks pilotes : useProductsWithEvents(userId) wrappe le service axios `getProducts` (forme objet useQuery, enabled si userId) — NB endpoint réel = GET /api/users/{userId}/products (events déjà embarqués dans Product), il n'existe PAS de /api/products/with-events côté backend (#70 intouché). useCurrentUser bridge AuthContext (#40) : queryFn relit `user` du contexte, ZÉRO appel réseau /me → pas de double-fetch ; documenté dans le hook + test asserte getUserProfile jamais appelé. Coexistence : SEULS ces 2 appels migrés, tout le reste (productService.createProduct, eventService, authService) reste axios brut inchangé. Tests : 12/12 (4 nouveaux hooks + suite #40 8 verte), tsc OK, 0 warning stderr.
- [MEMORY:*] signaux:
  - [MEMORY:pattern] Conventions query-keys: factory par domaine, clé liste préfixe de clé détail, `as const` → invalidateQueries ciblé. Anti-pattern: littéraux éparpillés.
  - [MEMORY:decision] Coexistence axios/TanStack: transport reste axios (queryFn appelle le service), TanStack n'ajoute que cache/dédup/refetch. Migration progressive, 2 pilotes seulement.
  - [MEMORY:decision] Anti double-fetch /me: AuthContext (#40) reste source unique user; useCurrentUser est un pont read-only sur le contexte (queryFn sans HTTP), NE PAS coupler aux écrans déjà sur useAuth().
  - [MEMORY:pitfall] v5 staleTime:Infinity + initialData fige la valeur du premier render (null si AuthContext pas encore rehydraté) et bloque tout re-run de queryFn → data reste null. Solution: placeholderData + enabled:!loading, queryFn relit l'état courant.
- recommandations suite: PAS de RECOMMAND_DB_EXPERT, PAS de RECOMMAND_TEST_RUNNER (suite frontend légère, run inline suffisant). Note pour Waves 3-5: useMutation + invalidateQueries(queryKeys.products.all) à câbler lors des drawers/listes; endpoint backend dédié products-with-events à créer côté #70 si besoin d'agrégat distinct.
STATUS: COMPLETED

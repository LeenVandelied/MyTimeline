[MINEUR] frontend/src/lib/legal-pages.ts:1133 - `LEGAL_SOURCE_LOCALE` export public mais 0 appelant hors module + fix: `const` non-exportée, réduit surface API.
[MINEUR] frontend/e2e/sprint-75-legal-pages.spec.ts:432-434 - `/de/*` teste "Zurück" (back) mais pas "Zurück zur Startseite" (backToHome), contrairement à fr/en + fix: ajouter l'assertion symétrique.
[MINEUR] frontend/i18n.ts:618 - `resolveLocale` replie sur `fr` pour casse différente ('FR') sans normalisation lowercase préalable + fix: si intentionnel (segments toujours lowercase via middleware regex), documenter le choix dans le commentaire existant.
[OK] resolveLocale/hasLocale — signature `hasLocale<T>(locales, candidate): candidate is T` de use-intl 4.13.2 vérifiée dans node_modules, usage conforme ; repli `fr` correct sur undefined/invalide/attrape-tout ; loadMessages(locale) intact, `app/[locale]/layout.tsx` toujours l'appelant, signature inchangée.
[OK] Parité i18n legal.json — 66/66/66/66 clés, sets identiques fr/en/es/de (vérifié par script, pas par confiance) ; `tableOfContents` et `disclaimerOriginalFrench` traduits distinctement dans les 4 locales (aucune recopie fr) ; `common.navigation.back`/`backToHome` idem.
[OK] Formatage date — `timeZone: 'UTC'` épinglé dans `formatLegalDate`, seul point de formatage (grep global, aucun autre site de rendu de `LEGAL_LAST_UPDATED_ISO`) ; test dédié au décalage de fuseau (Los_Angeles) présent et passant.
[OK] Accessibilité sommaire — chiffres romains dans `<span aria-hidden="true">` FRÈRE (pas parent) du `<a>`, nom accessible du lien = seul `t(titleKey)` ; `id` uniques par page (Set size == length, testé) ; `nav aria-label` posé.
[OK] Rendu serveur — aucun `'use client'` introduit dans page.tsx (privacy/terms) ni dans `legal-disclaimer.tsx`/`legal-table-of-contents.tsx` ; `tsc --noEmit` et `eslint` clean sur tous les fichiers touchés (vérifié en direct, pas recopié du briefing).
[OK] Fuite de périmètre — classes DS déjà en place (`bg-bg`, `text-ink`, `border-rule`) réutilisées telles quelles, `scroll-mt-24` est un utilitaire Tailwind standard (scroll-margin), pas un restyle.
[OK] Build & tests — `next build` rejoué via `rtk proxy` : 52/52 pages, `/privacy` `/terms` prérendues fr/en/es/de (contrôle direct, pas confiance aveugle au résumé rtk) ; `vitest run legal-pages.test.ts i18n-request-config.test.ts` rejoué : 64/64 verts.
VERDICT: 0 CRITIQUE / 0 MAJEUR / 3 MINEUR

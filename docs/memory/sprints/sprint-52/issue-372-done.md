commits: [92147d0e6d02698d911d2acc1577ef54f13e1572]
pack_lu: OUI — cp-frontend.md §Structure `frontend/` (+ briefing §« Les 3 pièges connus à documenter »)
resume: README.md racine créé (n'existait pas) — produit, écrans, stack, démarrage 1 commande, 4 pièges, tests, renvoi prod, licence. MESURÉ : `docker compose up -d` depuis un état sans .env et sans volume → 3 conteneurs, Flyway applique 15 migrations, `/actuator/health` UP, `/` 307 → `/fr` 200, register 201 → login 200 → /me 200. DÉDUIT (non mesuré) : durée du 1er build (machine en concurrence avec une autre pile Docker), comportement du conflit 5432 côté Linux/0.0.0.0. Aucun fichier source touché.
verification_navigateur: NON FAITE au navigateur — vérifs HTTP par curl sur la pile réellement démarrée (307 `/`→`/fr`, 200 `/fr`, 200 `/fr/register`, health UP, cycle register/login/me). Rendu visuel non contrôlé (livrable documentaire).
tests: `./scripts/test-quiet.sh frontend` → 92 fichiers / 825 tests passed, 0 failed, 14,2 s. Backend NON lancé (aucun .java au diff, suite Testcontainers lourde, Docker déjà saturé par ma pile) → RECOMMAND_TEST_RUNNER si le lead veut le vert backend.
premisses_infirmees:
  1. « HELP.md est le stub Spring Initializr » — FAUX, et pour une raison que l'architecte n'a pas vue : `find -iname HELP.md` → 0 résultat ET `git ls-files | grep -i help` → 0 résultat, parce que **`.gitignore` ligne 1 = `HELP.md`** (ignoré depuis le scaffold Initializr). AC4 déjà satisfaite, non traitée.
  2. « `docs/ops/deploiement-profils.md` n'existe pas → chemin fantôme » (affirmé par l'architecte) — **la réfutation de l'architecte est elle-même fausse**. Le fichier existe : `docs/runbook/deploiement-profils.md` (8,9 Ko, hub déploiement prod). L'architecte n'a regardé que `docs/ops/`. Le renvoi de l'issue est donc légitime, seul le répertoire était faux → README pointe vers `docs/runbook/`.
  3. Piège 5432 « conflit → override nécessaire » — imprécis. MESURÉ ici (macOS, Docker 29.2.1, PostgreSQL Homebrew 14.16 sur 127.0.0.1+::1) : le conteneur démarre **sans erreur**, Docker bind `*:5432` ; c'est `psql -h localhost` depuis l'hôte qui atteint le PG de la machine, pas le conteneur. Échec de bind seulement si le PG local écoute `0.0.0.0`. L'application n'est jamais concernée (réseau interne `postgres:5432`). Les deux cas sont documentés.
  4. Piège CORS E2E — l'issue dit « CORS sur :3000 produit une erreur ». Inverse : `application-dev.properties:35` fige l'allowlist **à** `:3000` ; c'est le front servi sur un AUTRE port (`:3100`) qui prend le 403 déguisé en rate-limit.
  5. 4ᵉ piège NON prévu par l'issue, trouvé en démarrant : la sonde du service `frontend` (`wget --spider http://localhost:3000`) échoue **en permanence** — `localhost` résout en `::1` dans le conteneur, Next n'écoute que sur `0.0.0.0:3000` (vérifié : `wget http://127.0.0.1:3000` → exit 0, `netstat` → `0.0.0.0:3000`). `docker compose ps` affiche donc `frontend unhealthy` alors que l'app répond 200. Aucun service ne dépend de cette sonde → la pile démarre quand même. Documenté comme piège n° 4 ; le correctif (`127.0.0.1` dans le healthcheck) touche `docker-compose.yml`, hors de mon périmètre d'écriture.
non_couvert:
  - **Pas de clone vierge réel.** J'ai démarré depuis le worktree (working tree partagé avec 2 agents), pas depuis un `git clone` neuf. Ce qui a été neutralisé : aucun `.env` à la racine (vérifié), volumes Docker détruits (`down -v`) avant le run de contrôle, projet Compose isolé (`-p mytimeline-readme`). Ce qui reste non prouvé : qu'un clone frais ne contient rien d'autre qui aide au démarrage.
  - **Durée du 1er build non chiffrable** : machine en concurrence avec une pile Docker EdelWheels (4 conteneurs) pendant tout le build ; `mvn dependency:go-offline` a dominé. Le README dit « plusieurs dizaines de minutes selon la machine », pas un chiffre.
  - **Aucune capture d'écran** : l'issue proposait « capture ou description » — j'ai fait la description. Aucune image ajoutée au dépôt.
  - **Parcours produit/événement non exercé** : j'ai prouvé register → login → /me, pas la création d'un produit ni d'un événement depuis l'UI.
  - **1er `docker compose up -d --build` anormal, non reproduit** : le conteneur `frontend` est resté en `Created` (log s'arrête sur « Starting », commande sortie 0). Un `up -d` ultérieur l'a démarré, et le run de contrôle après `down -v` a démarré les 3 en 6,2 s. Cause non identifiée — je ne l'ai pas documentée dans le README faute de reproduction.
  - **Backend, E2E, lint, build Next non lancés.**
  - `frontend/.eslintcache` apparaît supprimé dans `git status` : travail d'un autre agent, ni committé ni reverté par moi.
[MEMORY:pitfall] Contexte : sonde `frontend` du docker-compose (`wget --spider http://localhost:3000`) marque le service `unhealthy` à vie alors que l'app sert du 200. Cause : `/etc/hosts` du conteneur mappe `localhost` sur `127.0.0.1` ET `::1` ; BusyBox wget tente `::1` d'abord ; Next standalone n'écoute que sur `0.0.0.0:3000` (IPv4). Solution : cibler `127.0.0.1` dans le healthcheck. Prévention : toute sonde `wget/curl localhost` dans une image alpine doit viser `127.0.0.1` explicitement.
[MEMORY:pitfall] Contexte : 5ᵉ sprint de chemins fantômes — mais cette fois le **plan de l'architecte** a produit le faux négatif (« `deploiement-profils.md` n'existe pas ») en cherchant dans un seul répertoire. Solution : `find docs -name "<fichier>"` avant de déclarer un chemin inexistant, jamais un `ls` d'un seul dossier. Prévention : une réfutation de prémisse doit citer la commande qui balaye tout l'arbre.
[MEMORY:decision] Contexte : `HELP.md` introuvable. Décision : ne pas le recréer ni le traiter — il est dans `.gitignore` ligne 1 depuis le scaffold Spring Initializr. Pourquoi : AC4 (« HELP.md supprimé ou vidé ») est structurellement satisfaite ; toute action dessus serait du bruit.
recommandations suite: RECOMMAND_TEST_RUNNER (suite backend non lancée) · follow-up à ouvrir : corriger le healthcheck `frontend` de `docker-compose.yml` en `http://127.0.0.1:3000` (1 ligne, hors périmètre #372) · follow-up mineur : `frontend/README.md` est encore le stub `create-next-app` (mentionne `app/page.tsx` et Vercel), il contredit le README racine.
STATUS: COMPLETED
